extends:
    "./eslint/common.yml"

rules:
    no-var: "error"
    prefer-const: "warn"
    require-yield: "error"
    arrow-spacing: "error"
    no-confusing-arrow: "error"
    strict: ["error", "global"]
